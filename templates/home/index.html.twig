{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('user_index') }}">Users</a>
    <a href="{{ path('team_index') }}">Teams</a>
    <a href="{{ path('server_index') }}">Servers</a>
{% endblock %}
